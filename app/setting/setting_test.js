'use strict';

describe('myApp.setting module', function() {

  beforeEach(module('myApp.setting'));

  describe('setting controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var settingCtrl = $controller('settingCtrl');
      expect(settingCtrl).toBeDefined();
    }));

  });
});
