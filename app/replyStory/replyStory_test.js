'use strict';

describe('myApp.replyStory module', function() {

  beforeEach(module('myApp.replyStory'));

  describe('replyStory controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var replyStoryCtrl = $controller('replyStoryCtrl');
      expect(replyStoryCtrl).toBeDefined();
    }));

  });
});
